name: Facility CI

on:
  push:
    branches:
      - main
      - "codex/**"
  pull_request:
    branches:
      - main

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npx tsc --noEmit --pretty false

      - name: Verify generated Camera Core
        run: npm run verify:camera-core-distribution && npm run smoke:camera-runtime-canonicalization && npm run smoke:camera-center

      - name: Build
        run: npm run build

      - name: Facility activation wizard smoke
        run: npm run smoke:facility-activation-wizard

      - name: Validate release gate
        run: npm run validate:release
