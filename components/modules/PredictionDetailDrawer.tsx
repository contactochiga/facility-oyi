"use client";

import { useEffect, useMemo, useState } from "react";
import { CheckCircle2, Loader2, X } from "lucide-react";
import { facilityService } from "@/services/facilityService";

const text = (value: unknown, fallback = "Unavailable") => String(value || fallback).replace(/_/g, " ");
const date = (value?: unknown) => value ? new Date(String(value)).toLocaleString([], { month: "short", day: "numeric", hour: "2-digit", minute: "2-digit" }) : "Not recorded";
const confidenceLabel = (value: unknown) => ({ confirmed: "High", likely: "High", possible: "Moderate", needs_monitoring: "Monitor" }[String(value || "").toLowerCase()] || "Unavailable");

export function openPredictionDrawer(prediction: any) {
  if (!prediction || typeof window === "undefined") return;
  window.dispatchEvent(new CustomEvent("facility:open-prediction", { detail: { prediction } }));
}

export function PredictionDetailDrawerHost() {
  const [prediction, setPrediction] = useState<any | null>(null);
  useEffect(() => { const handler = (event: Event) => setPrediction((event as CustomEvent<{ prediction?: any }>).detail?.prediction || null); window.addEventListener("facility:open-prediction", handler); return () => window.removeEventListener("facility:open-prediction", handler); }, []);
  return prediction ? <PredictionDetailDrawer prediction={prediction} onClose={() => setPrediction(null)} /> : null;
}

export default function PredictionDetailDrawer({ prediction: initial, onClose }: { prediction: any; onClose: () => void }) {
  const [prediction, setPrediction] = useState(initial); const [acknowledging, setAcknowledging] = useState(false); const [error, setError] = useState<string | null>(null);
  useEffect(() => setPrediction(initial), [initial]);
  const evidence = Array.isArray(prediction?.evidence) ? prediction.evidence : [];
  const impact = prediction?.metadata?.impact || prediction?.impact || prediction?.metadata?.impact_summary || null;
  const category = text(prediction?.prediction_type, "Operational risk");
  const acknowledgement = useMemo(() => String(prediction?.status || "open").toLowerCase() === "acknowledged", [prediction?.status]);
  const acknowledge = async () => { if (!prediction?.id || acknowledgement || acknowledging) return; setAcknowledging(true); setError(null); try { const result = await facilityService.acknowledgePrediction(String(prediction.id)); if (!result?.ok && !result?.prediction) throw new Error("acknowledgement failed"); setPrediction(result.prediction || { ...prediction, status: "acknowledged" }); } catch { setError("Unable to acknowledge this prediction. No acknowledgement was recorded."); } finally { setAcknowledging(false); } };
  return <div className="fixed inset-0 z-[71] bg-black/65 backdrop-blur-sm"><aside className="ml-auto flex h-[100dvh] w-full max-w-[640px] flex-col border-l border-white/10 bg-zinc-950 shadow-2xl"><header className="sticky top-0 z-10 flex items-start justify-between border-b border-white/10 bg-zinc-950/95 p-4 backdrop-blur"><div className="min-w-0"><p className="text-[10px] uppercase tracking-[0.16em] text-sky-200/75">Prediction detail</p><h2 className="mt-1 truncate text-lg font-semibold text-white">{prediction?.title || "Prediction unavailable"}</h2><p className="mt-1 text-xs text-zinc-500">{category} · {date(prediction?.created_at)}</p></div><button onClick={onClose} className="grid h-9 w-9 place-items-center rounded-full border border-white/10 bg-white/[0.04]" aria-label="Close prediction detail"><X className="h-4 w-4 text-zinc-300" /></button></header><div className="flex-1 overflow-y-auto p-4 pb-28"><div className="flex flex-wrap gap-2"><span className="rounded-full border border-amber-400/20 bg-amber-500/10 px-2 py-1 text-[10px] uppercase text-amber-100">{text(prediction?.severity, "risk")}</span><span className="rounded-full border border-white/10 bg-white/[0.05] px-2 py-1 text-[10px] uppercase text-zinc-300">{text(prediction?.status, "active")}</span></div><section className="mt-5 rounded-xl border border-white/10 bg-black/20 p-3"><h3 className="text-sm font-medium text-white">Confidence and impact</h3><div className="mt-3 grid grid-cols-3 gap-2 text-xs"><div><span className="block text-zinc-500">Confidence</span><span className="text-zinc-200">{confidenceLabel(prediction?.confidence)}</span></div><div><span className="block text-zinc-500">Risk</span><span className="text-zinc-200">{text(prediction?.severity, "Unspecified")}</span></div><div><span className="block text-zinc-500">Impact</span><span className="text-zinc-200">{impact ? text(impact) : "Not specified"}</span></div></div></section><section className="mt-3 rounded-xl border border-white/10 bg-black/20 p-3"><h3 className="text-sm font-medium text-white">Why Oyi flagged this</h3><p className="mt-2 text-sm leading-6 text-zinc-300">{prediction?.summary || "Prediction details are currently unavailable."}</p></section><section className="mt-3 rounded-xl border border-white/10 bg-black/20 p-3"><h3 className="text-sm font-medium text-white">Supporting evidence</h3><div className="mt-3 space-y-2">{evidence.slice(0, 15).map((item: any, index: number) => <div key={index} className="rounded-lg border border-white/10 p-2 text-xs"><b className="block text-zinc-200">{item.title || item.source || item.source_table || "Supporting signal"}</b><p className="mt-1 text-zinc-500">{text(item.event_type, "Recorded event")} · {date(item.occurred_at || item.created_at || item.updated_at)}</p></div>)}{!evidence.length ? <p className="text-xs text-zinc-500">No supporting evidence was returned for this prediction.</p> : null}</div></section><section className="mt-3 rounded-xl border border-white/10 bg-black/20 p-3"><h3 className="text-sm font-medium text-white">Recommended next action</h3><p className="mt-2 text-sm text-sky-100">{prediction?.recommended_action || "No recommended action is currently available."}</p></section><section className="mt-3 rounded-xl border border-white/10 bg-black/20 p-3"><h3 className="text-sm font-medium text-white">Acknowledgement</h3><p className="mt-2 text-xs text-zinc-400">{acknowledgement ? `Acknowledged ${date(prediction?.acknowledged_at)}` : "Not acknowledged"}</p>{acknowledgement && prediction?.acknowledged_by ? <p className="mt-1 text-xs text-zinc-500">Acknowledgement owner recorded.</p> : null}</section>{error ? <p className="mt-3 rounded-lg border border-rose-400/20 bg-rose-500/10 p-2 text-xs text-rose-100">{error}</p> : null}</div><footer className="sticky bottom-0 border-t border-white/10 bg-zinc-950/95 p-4 backdrop-blur">{acknowledgement ? <div className="flex items-center gap-2 text-sm text-emerald-100"><CheckCircle2 className="h-4 w-4" />Prediction acknowledged</div> : <button disabled={acknowledging} onClick={() => void acknowledge()} className="inline-flex items-center gap-2 rounded-lg bg-sky-500/15 px-3 py-2 text-sm text-sky-100 disabled:opacity-50">{acknowledging ? <Loader2 className="h-4 w-4 animate-spin" /> : <CheckCircle2 className="h-4 w-4" />}{acknowledging ? "Acknowledging" : "Acknowledge prediction"}</button>}</footer></aside></div>;
}
