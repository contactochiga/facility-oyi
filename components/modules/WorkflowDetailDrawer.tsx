"use client";

import { useEffect, useMemo, useState } from "react";
import { CheckCircle2, Loader2 } from "lucide-react";
import { facilityService } from "@/services/facilityService";
import OisCard from "@/components/ois/OisCard";
import OisDrawer from "@/components/ois/OisDrawer";
import OisListItem from "@/components/ois/OisListItem";
import OisStatusBadge, { type OisStatus } from "@/components/ois/OisStatusBadge";
import VerificationBadge, { normalizeVerificationState } from "@/components/modules/VerificationBadge";

type WorkflowDetailDrawerProps = { workflowId: string; onClose: () => void };
const closed = new Set(["completed", "resolved", "verified", "closed", "cancelled"]);
const text = (value: unknown, fallback = "Unavailable") => String(value || fallback).replace(/_/g, " ");
const date = (value?: unknown) => value ? new Date(String(value)).toLocaleString([], { month: "short", day: "numeric", hour: "2-digit", minute: "2-digit" }) : "Not recorded";
const age = (value?: unknown) => { if (!value) return "Age unavailable"; const hours = Math.max(0, Math.floor((Date.now() - new Date(String(value)).getTime()) / 3600000)); return hours < 24 ? `${hours}h old` : `${Math.floor(hours / 24)}d old`; };
const normalizeStatus = (value: unknown): OisStatus => {
  const state = String(value || "").toLowerCase().trim().replace(/[\s-]+/g, "_");
  if (["stable", "attention", "warning", "critical", "unavailable", "pending", "verified", "failed", "escalated", "blocked", "overdue", "resolved", "completed"].includes(state)) return state as OisStatus;
  return "unavailable";
};

export function openWorkflowDrawer(workflowId?: string | null) {
  if (!workflowId || typeof window === "undefined") return;
  window.dispatchEvent(new CustomEvent("facility:open-workflow", { detail: { workflowId } }));
}

export function WorkflowDetailDrawerHost() {
  const [workflowId, setWorkflowId] = useState<string | null>(null);
  useEffect(() => { const handler = (event: Event) => setWorkflowId((event as CustomEvent<{ workflowId?: string }>).detail?.workflowId || null); window.addEventListener("facility:open-workflow", handler); return () => window.removeEventListener("facility:open-workflow", handler); }, []);
  return workflowId ? <WorkflowDetailDrawer workflowId={workflowId} onClose={() => setWorkflowId(null)} /> : null;
}

export default function WorkflowDetailDrawer({ workflowId, onClose }: WorkflowDetailDrawerProps) {
  const [detail, setDetail] = useState<any | null>(null); const [events, setEvents] = useState<any[]>([]); const [state, setState] = useState<"loading" | "error" | "ready">("loading");
  useEffect(() => { let mounted = true; setState("loading"); facilityService.intelligenceWorkflow(workflowId).then((data) => { if (!mounted) return; if (!data?.ok && !data?.workflow) throw new Error("not found"); setDetail(data.workflow || data); setEvents(Array.isArray(data.events) ? data.events : []); setState("ready"); }).catch(() => mounted && setState("error")); return () => { mounted = false; }; }, [workflowId]);
  const status = String(detail?.workflow_status || detail?.status || "created").toLowerCase();
  const overdue = Boolean(detail?.workflow_due_at && new Date(detail.workflow_due_at).getTime() < Date.now() && !closed.has(status));
  const verification = detail?.metadata?.verification_state || detail?.verification_state || (["completed", "resolved"].includes(status) ? "pending" : null);
  const recommendation = useMemo(() => { if (!detail) return "Review workflow details."; if (verification && String(verification).toLowerCase() !== "verified") return "Review and verify completion."; if (overdue && !(detail.workflow_assignee || detail.assignee)) return "Assign an owner and escalate if the SLA remains at risk."; if (detail.workflow_resolution || detail.blocking_reason || status === "blocked") return "Review the blocker before continuing the workflow."; if (status === "failed") return "Review failure evidence and determine the next safe action."; if (status === "completed") return "Confirm closure if the outcome is verified."; return detail.workflow_assignee || detail.assignee ? "Review ownership and the next operational step." : "Assign an owner."; }, [detail, overdue, status, verification]);
  const evidence = Array.isArray(detail?.evidence) ? detail.evidence : Array.isArray(detail?.metadata?.evidence) ? detail.metadata.evidence : [];
  const owner = detail?.workflow_owner || detail?.owner || null; const assignee = detail?.workflow_assignee || detail?.assignee || null;
  return <OisDrawer open={true} onClose={onClose} width="md" title={detail?.title || "Loading workflow"} subtitle={`${text(detail?.workflow_type, "Operational workflow")} · ${date(detail?.created_at)}`} footer={state === "ready" && detail ? <div className="flex items-center gap-2"><CheckCircle2 className="h-4 w-4 text-sky-200" /><div><p className="text-xs text-zinc-500">Recommended next action</p><p className="text-sm text-sky-100">{recommendation}</p></div></div> : undefined}>
    {state === "loading" ? <div className="flex items-center gap-2 text-sm text-zinc-500"><Loader2 className="h-4 w-4 animate-spin" />Loading workflow detail</div> : null}
    {state === "error" ? <p className="rounded-xl border border-rose-400/20 bg-rose-500/10 p-3 text-sm text-rose-100">This workflow is unavailable or you do not have access to it.</p> : null}
    {state === "ready" && detail ? <><OisCard as="section" variant="evidence" className="p-3"><div className="flex flex-wrap gap-2"><OisStatusBadge status={normalizeStatus(status)} label={text(status)} /><OisStatusBadge status={normalizeStatus(detail.workflow_priority || detail.priority || "attention")} label={text(detail.workflow_priority || detail.priority, "medium")} />{overdue ? <OisStatusBadge status="overdue" /> : null}{status === "escalated" ? <OisStatusBadge status="escalated" /> : null}<VerificationBadge state={normalizeVerificationState(verification, overdue && Boolean(verification))} /></div></OisCard><OisCard as="section" variant="evidence" className="mt-3 p-3"><h3 className="text-sm font-medium text-white">Ownership</h3><div className="mt-3 grid grid-cols-2 gap-2 text-xs"><div><span className="block text-zinc-500">Owner</span><span className="text-zinc-200">{owner || "Owner unavailable"}</span></div><div><span className="block text-zinc-500">Assignee</span><span className="text-zinc-200">{assignee || "Not assigned"}</span></div><div><span className="block text-zinc-500">Created by</span><span className="text-zinc-200">{detail.created_by || "Unavailable"}</span></div><div><span className="block text-zinc-500">Assigned at</span><span className="text-zinc-200">{date(detail.assigned_at)}</span></div><div><span className="block text-zinc-500">Verification owner</span><span className="text-zinc-200">{detail.verified_by || "Unavailable"}</span></div></div></OisCard><OisCard as="section" variant="evidence" className="mt-3 p-3"><h3 className="text-sm font-medium text-white">Lifecycle state</h3><div className="mt-3 grid grid-cols-2 gap-2 text-xs"><div><span className="block text-zinc-500">Age</span><span className="text-zinc-200">{age(detail.created_at)}</span></div><div><span className="block text-zinc-500">Due state</span><span className="text-zinc-200">{detail.workflow_due_at ? overdue ? `Overdue by ${age(detail.workflow_due_at).replace("old", "")}` : `Due ${date(detail.workflow_due_at)}` : "No due time"}</span></div><div><span className="block text-zinc-500">Updated</span><span className="text-zinc-200">{date(detail.updated_at)}</span></div><div><span className="block text-zinc-500">Resolution</span><span className="text-zinc-200">{detail.workflow_resolution || "No resolution recorded"}</span></div></div>{detail.blocking_reason ? <p className="mt-3 rounded-lg border border-amber-400/20 bg-amber-500/10 p-2 text-xs text-amber-100">Blocked: {detail.blocking_reason}</p> : null}</OisCard><OisCard as="section" variant="evidence" className="mt-3 p-3"><h3 className="text-sm font-medium text-white">Verification</h3><div className="mt-3 grid grid-cols-2 gap-2 text-xs"><div><span className="block text-zinc-500">Status</span><VerificationBadge state={normalizeVerificationState(verification, overdue && Boolean(verification))} /></div><div><span className="block text-zinc-500">Verified at</span><span className="text-zinc-200">{date(detail.verified_at)}</span></div><div><span className="block text-zinc-500">Verified by</span><span className="text-zinc-200">{detail.verified_by || "Unavailable"}</span></div><div><span className="block text-zinc-500">Outcome</span><span className="text-zinc-200">{detail.metadata?.verification_summary || detail.workflow_resolution || "No verification outcome recorded"}</span></div></div></OisCard><OisCard as="section" variant="evidence" className="mt-3 p-3"><h3 className="text-sm font-medium text-white">Activity</h3><div className="mt-3 space-y-2">{events.slice(0, 15).map((event) => <OisListItem key={event.id || `${event.event_type}-${event.occurred_at}`} title={text(event.event_type || event.to_status, "Workflow update")} description={event.summary || event.note || "No narrative note recorded."} meta={date(event.occurred_at || event.created_at)} status={normalizeStatus(event.to_status || event.event_type || "unavailable")} />)}{!events.length ? <p className="text-xs text-zinc-500">No workflow activity is available yet.</p> : null}</div></OisCard><OisCard as="section" variant="evidence" className="mt-3 p-3"><h3 className="text-sm font-medium text-white">Evidence</h3><div className="mt-3 space-y-2">{evidence.slice(0, 15).map((item: any, index: number) => <OisListItem key={item.id || `${item.source || item.source_table || "evidence"}-${index}`} title={item.title || item.label || item.source || item.source_table || "Supporting signal"} description={item.summary || "No narrative note recorded."} meta={item.occurred_at || item.created_at || item.updated_at ? date(item.occurred_at || item.created_at || item.updated_at) : "Timestamp unavailable"} status="stable" />)}{!evidence.length ? <p className="text-xs text-zinc-500">No supporting evidence was returned for this workflow.</p> : null}</div></OisCard></> : null}
  </OisDrawer>;
}
