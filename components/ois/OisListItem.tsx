import type { ReactNode } from "react";
import OisStatusBadge, { type OisStatus } from "./OisStatusBadge";

export default function OisListItem({ title, description, meta, status, priority, icon, action, onClick, className = "" }: { title: ReactNode; description?: ReactNode; meta?: ReactNode; status?: OisStatus | string; priority?: ReactNode; icon?: ReactNode; action?: ReactNode; onClick?: () => void; className?: string }) {
  const interactive = Boolean(onClick);
  return <div role={interactive ? "button" : undefined} tabIndex={interactive ? 0 : undefined} onClick={onClick} onKeyDown={(event) => { if (interactive && (event.key === "Enter" || event.key === " ")) onClick?.(); }} className={`ois-registry-row flex min-h-[68px] items-start gap-3 rounded-[var(--ois-radius-row)] border border-transparent px-3 py-2.5 transition-all duration-150 ${interactive ? "cursor-pointer hover:border-white/[0.05] hover:bg-white/[0.03] active:scale-[0.995] focus-visible:border-sky-400/30 focus-visible:bg-white/[0.03]" : ""} ${className}`.trim()}>{icon ? <span className="grid h-9 w-9 shrink-0 place-items-center rounded-[10px] bg-white/[0.04] text-[var(--ois-text-primary)]">{icon}</span> : null}<div className="min-w-0 flex-1"><div className="flex flex-wrap items-start justify-between gap-2"><h3 className="min-w-0 text-[14px] font-medium leading-[1.1rem] text-[var(--ois-text-primary)]">{title}</h3>{status ? <OisStatusBadge status={status} size="md" /> : priority ? <span className="text-[var(--ois-type-meta)] text-[var(--ois-text-secondary)]">{priority}</span> : null}</div>{description ? <p className="mt-0.5 text-[12px] leading-4 text-[var(--ois-text-secondary)]">{description}</p> : null}{meta ? <div className="mt-1 text-[10px] leading-4 text-[var(--ois-text-muted)]">{meta}</div> : null}</div>{action ? <span className="shrink-0 pt-0.5">{action}</span> : null}</div>;
}
