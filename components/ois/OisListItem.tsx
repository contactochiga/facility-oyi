import type { ReactNode } from "react";
import OisStatusBadge, { type OisStatus } from "./OisStatusBadge";

export default function OisListItem({ title, description, meta, status, priority, icon, action, onClick, className = "" }: { title: ReactNode; description?: ReactNode; meta?: ReactNode; status?: OisStatus | string; priority?: ReactNode; icon?: ReactNode; action?: ReactNode; onClick?: () => void; className?: string }) {
  const interactive = Boolean(onClick);
  return <div role={interactive ? "button" : undefined} tabIndex={interactive ? 0 : undefined} onClick={onClick} onKeyDown={(event) => { if (interactive && (event.key === "Enter" || event.key === " ")) onClick?.(); }} className={`flex items-start gap-[var(--ois-space-3)] rounded-[var(--ois-radius-row)] border border-[var(--ois-border-subtle)] bg-[var(--ois-surface-evidence)] p-[var(--ois-space-3)] ${interactive ? "cursor-pointer transition duration-[var(--ois-motion-standard)] hover:bg-[var(--ois-surface-pressed)]" : ""} ${className}`.trim()}>{icon ? <span className="grid h-9 w-9 shrink-0 place-items-center rounded-[var(--ois-radius-control)] bg-[var(--ois-primary-soft)] text-[var(--ois-primary)]">{icon}</span> : null}<div className="min-w-0 flex-1"><div className="flex flex-wrap items-start justify-between gap-2"><h3 className="min-w-0 text-[var(--ois-type-card-title)] font-medium leading-[var(--ois-line-card-title)] text-[var(--ois-text-primary)]">{title}</h3>{status ? <OisStatusBadge status={status} /> : priority ? <span className="text-[var(--ois-type-meta)] text-[var(--ois-text-secondary)]">{priority}</span> : null}</div>{description ? <p className="mt-1 text-[var(--ois-type-body-small)] leading-[var(--ois-line-body-small)] text-[var(--ois-text-secondary)]">{description}</p> : null}{meta ? <div className="mt-2 text-[var(--ois-type-meta)] leading-[var(--ois-line-meta)] text-[var(--ois-text-muted)]">{meta}</div> : null}</div>{action ? <span className="shrink-0">{action}</span> : null}</div>;
}
