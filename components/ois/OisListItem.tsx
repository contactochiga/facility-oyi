import type { ReactNode } from "react";
import OisStatusBadge, { type OisStatus } from "./OisStatusBadge";

export default function OisListItem({ title, description, meta, status, priority, icon, action, onClick, className = "" }: { title: ReactNode; description?: ReactNode; meta?: ReactNode; status?: OisStatus | string; priority?: ReactNode; icon?: ReactNode; action?: ReactNode; onClick?: () => void; className?: string }) {
  const interactive = Boolean(onClick);
  return <div role={interactive ? "button" : undefined} tabIndex={interactive ? 0 : undefined} onClick={onClick} onKeyDown={(event) => { if (interactive && (event.key === "Enter" || event.key === " ")) onClick?.(); }} className={`flex items-start gap-[var(--ois-space-3)] rounded-[18px] border border-white/[0.06] bg-white/[0.02] px-[var(--ois-space-3)] py-[10px] ${interactive ? "cursor-pointer transition duration-[var(--ois-motion-standard)] hover:border-white/[0.1] hover:bg-white/[0.038]" : ""} ${className}`.trim()}>{icon ? <span className="grid h-9 w-9 shrink-0 place-items-center rounded-[var(--ois-radius-control)] bg-[var(--ois-primary-soft)] text-[var(--ois-primary)]">{icon}</span> : null}<div className="min-w-0 flex-1"><div className="flex flex-wrap items-start justify-between gap-2"><h3 className="min-w-0 text-[0.875rem] font-medium leading-5 text-[var(--ois-text-primary)]">{title}</h3>{status ? <OisStatusBadge status={status} /> : priority ? <span className="text-[var(--ois-type-meta)] text-[var(--ois-text-secondary)]">{priority}</span> : null}</div>{description ? <p className="mt-1 text-[var(--ois-type-body-small)] leading-[1.2rem] text-[var(--ois-text-secondary)]">{description}</p> : null}{meta ? <div className="mt-1.5 text-[11px] leading-4 text-[var(--ois-text-muted)]">{meta}</div> : null}</div>{action ? <span className="shrink-0">{action}</span> : null}</div>;
}
