# Facility Environment Variable Checklist

- backend base URL for API access
- facility public origin/domain
- analytics keys only when analytics is enabled
- any native shell environment needed for Capacitor sync targets
