# Facility Deployment Checklist

- `npm run lint`
- `npx tsc --noEmit --pretty false`
- `npm run build`
- `npm run validate:release`
- Confirm backend base URL and auth cookie configuration for the target environment.
- Confirm shell navigation, authenticated routes, and realtime subscriptions after deployment.
